import React from "react";

type Props = {
  content: string;
  className?: string;
  onClick?: () => void;
  urlPath?: string;
};

function Btn({}: Props) {
  return <button></button>;
}

export default Btn;
