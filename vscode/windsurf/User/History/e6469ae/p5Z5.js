export function breakMcqIntoParts(questionsArray) {
  const questions = [];
  questionsArray.forEach(async (quiz) => {
    const { question, choices, answer, answerExplanation, difficulty } = quiz;
  });
}
