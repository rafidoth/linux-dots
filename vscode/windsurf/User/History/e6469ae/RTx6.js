export function breakGeneratedMcqQuestionsIntoParts(questionsArray) {
  const questions = [];
  quizzes.forEach(async (quiz) => {
    const { question, choices, answer, answerExplanation, difficulty } = quiz;
  });
}
